-- Причина проживания для нанимателей не из АНО ВО «РосНОУ» — печатается в п.1.2
-- бланка договора вместо формулировки "обучением в АНО ВО «РосНОУ»» (см. schema.prisma).

ALTER TABLE "contracts" ADD COLUMN "residence_reason" TEXT;
