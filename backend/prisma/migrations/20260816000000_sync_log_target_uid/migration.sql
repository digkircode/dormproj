-- UID физлица для точечной синхронизации одного человека с его карточки —
-- заполняется только у этого типа синхрона, у остальных пяти всегда NULL.
ALTER TABLE "sync_logs" ADD COLUMN "target_uid" TEXT;
