export const SYNC_TYPE_STUDENTS = 'students';

// Нормальный sync на наших объёмах занимает секунды-десятки секунд. Если лок висит
// дольше — процесс, скорее всего, упал, не дойдя до finally, и лок можно перехватить.
export const LOCK_STALE_MS = 30 * 60 * 1000;

// Не выполняем hard delete, если пришло меньше половины от текущего количества
// студентов в базе — это с высокой вероятностью баг источника, а не массовый отчисление.
export const MIN_SURVIVAL_RATIO = 0.5;

export const TRANSACTION_TIMEOUT_MS = 120_000;

// Ждём sync раз в сутки; если последнего успешного не было дольше — вероятно,
// плановый запуск был пропущен (например, контейнер перезапустился в момент cron).
export const MISSED_RUN_THRESHOLD_MS = 26 * 60 * 60 * 1000;
